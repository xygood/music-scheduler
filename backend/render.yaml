services:
  - type: web
    name: music-scheduler-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python teacher_management.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.9"
